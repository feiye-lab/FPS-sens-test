# 灵敏度测试工具 — 进展与待办

工具本体：**`sens-test.html`**（单文件，无依赖，Chrome/Edge 打开）
一个文件同时支持 **CS2** 和 **无畏契约**，顶部切换，两边设置各存一份、互不覆盖。

---

## 三种模式

| 模式 | 回答什么 | 能不能信 |
|---|---|---|
| 匹配度测试 | 当前灵敏度合不合手 | 只能判**有没有错配**和**方向**，给不出准确数值 |
| 灵敏度对比 A/B | 两个灵敏度哪个表现更好 | **可信**，四维记分卡 + Holm 校正 |
| 新手找灵敏度 | 还没有固定值时选哪个 | **可信**，2–4 档自定义扫描 |

**核心限制**：人在十几次甩枪内就会适应任何灵敏度，所以"匹配度"只在最开始几次里有信号。要可信的推荐值，用 A/B 或扫描模式。

---

## 两个游戏差在哪

测量内核（落点比值、Welch 检验、Holm 校正、适应曲线、波次归一化）是纯角度量，**与引擎无关，一行都没分支**。真正不同的只有三样：

### 1. 每计数转角 → 灵敏度可精确互换

| | 每计数转角 |
|---|---|
| CS2（Source） | 0.022° |
| 无畏契约 | 0.07° |

```
无畏契约灵敏度 = CS 灵敏度 × 0.022 / 0.07 = CS 灵敏度 ÷ 3.18182
```

DPI 在等式里自动抵消，所以换算与 DPI 无关。界面上的「按手感换算过来」按钮就是干这个的，实测 CS 0.6 → 无畏契约 0.1886，两边都是 43.3 cm/360。

**不能换算的**：FOV（16:9 下 106.26° vs 103°）。业界标准换算就是按 360° 对齐，不补偿 FOV 差异——补偿反而会让肌肉记忆的转身距离对不上。

### 2. FOV 模型 —— 不是数值不同，是缩放方式不同

| | CS2 | 无畏契约 |
|---|---|---|
| 模型 | Hor+ | 固定 FOV + 拉伸 |
| 垂直 | 恒定 73.74° | 恒定 70.53° |
| 水平 | 随比例变（4:3 → 90°，16:9 → 106.26°） | **恒定 103°** |
| 4:3 拉伸铺满 | 牺牲视野换模型变宽 | **和 16:9 原生完全一样**，纯白拉 |
| 4:3 黑边 | 视野变窄，方形像素 | 画面被压窄到 0.750×，模型更**细** |

无畏契约那栏的依据：[KovaaK's FILM 记法文档](https://www.kovaak.com/film-notation/)原话——基准画面 16:9，"任何比例变化只会拉伸或压缩画面，同时保持相同的水平和垂直 FOV"。

代码上 `hFovOf(a)` 按 `G.fovMode` 分支；投影公式共用，已验证水平视口边缘恒在 hFOV/2、垂直恒在 vFOV/2。

**待核实：无畏契约的 103° 是社区通用值，没有实机验证过。** 它只影响目标的视觉大小，不影响落点比值（纯角度量），所以不阻塞结论。要定死：进游戏对着已知宽度的物体量一下即可。

### 3. 准星 —— 两套毫无共通之处的系统

| | CS2 | 无畏契约 |
|---|---|---|
| 编码 | base-57 大整数 + 18 字节位域 + 校验和 | 分号分隔的「键;值」明文串，无校验和 |
| 结构 | 单组臂 | 内线 + 外线两组独立参数 |
| 省略默认值 | 否 | **是** —— 全默认的准星代码就是一个字符 `0` |
| 几何 | `size×2`、`gap+4`、`size>2 再 +1` 一堆怪癖 | 参数就是字面像素值，没有任何换算 |
| 导出 | 分享代码 + 控制台命令 | 只有准星代码（游戏没控制台） |

**把任一边的换算搬到另一边都会画错**——CS 那套用到无畏契约上会大一倍。

参考实现：CS2 对照 `akiver/csgo-sharecode`（6 组官方向量全过）+ `kstdnr/csgocfgr_client`（几何，8/8 配置一致）；无畏契约对照 `genesy/crosshair-codes` 的 `codegenerator.ts` / `crosshair.tsx` / `CrosshairDisplay.tsx`。

无畏契约有两处**以实测代码为准、偏离了参考实现**：

1. 线段内部顺序，参考写的是 `…a, f, e, m, s`，但真实代码 `1t;0;1l;0;1o;0;1a;0;1m;0;1f;0` 里 **m 在 f 前面**。
2. 主准星的 `f`（开火时淡出）参考实现漏了建模，真实代码里有，位置在中心点之后、线段之前。`f` 与 `m` 的先后没有实例佐证，暂按游戏设置界面的顺序放。

不认识的键**不报错**，原样保留原样写回（Riot 加过新键，炸掉会让一批真实代码用不了）。A/S/NAME 段整段原文保留。

**验证**：无畏契约 11 组代码往返完全一致（含实测代码、自定义颜色、拆分纵横长度、高级选项标志位、带 ADS 段的代码）；CS2 用户自己的 `CSGO-OUHO2-bWakt-2EThQ-JJisR-6sWPA` 往返字节一致。切游戏来回一趟后两边准星都不受影响。

### 4. 压枪阶段（仅 CS2）

CS 比无畏契约多一个**压枪测试**（第 7 步）。为什么只给 CS 加、不给无畏契约加东西：

- **无畏契约强调的一次定位，已经是这个工具的核心测量**——落点比值测的就是首次停枪落在哪。再加一个“只准点一枪”的阶段是重复测量。
- **压枪真的没被测到**，而且它是唯一一个**不允许抬鼠标复位**的动作：一梭子必须一路拉到底。其余七个阶段都能中途重新握位，低灵敏度从不吃亏。
- 后坐力补偿要走的物理距离 = 角度 ÷ (每计数转角 × 灵敏度 × DPI)，和 cm/360 严格成正比，所以它确实是灵敏度的函数。

**后坐力刻意不是 AK 的真实弹道。** 真实弹道背得下来，而背下来的那套是绑定在你当时那个灵敏度上的——换个灵敏度测，成绩变差会是因为“记忆对不上”而不是“灵敏度不合适”，正是这个工具一直在躲的适应性混淆。所以拆成两半：

| | 做法 | 理由 |
|---|---|---|
| 纵向 | **固定** profile，累计抬升 22° | 它才是要测的能力（能不能稳定执行一段持续下拉），A/B 两边必须一致 |
| 横向 | **每段随机**频率 + 随机左右，摆幅 8° | 背不住，只能看着补，测的是闭环修正 |

形状按社区公认的定性描述（前 4–5 发几乎直上，之后转横向摆动），射速 600 发/分。

**抬升曲线用临界阻尼阶跃响应** `1-(1+x)e^(-x)`，x = n/`SPRAY_TAU`（τ=3.5 发）。
最初用的是 `1-e^(-x)`，但那条曲线的**最大斜率恰好在 n=0**——一扣扳机就是最猛的一下，用户反馈“刚开枪那一下向上抖动太严重”就是这个。实机也不是这样：CS 里第一发是准的，后坐力是打出去之后才累积起来的。新曲线在 n=0 处**值和斜率都是 0**，先加速再饱和：

| | 旧 `1-e^(-n/4.2)` | 新 `1-(1+x)e^(-x)`，τ=3.5 |
|---|---|---|
| 开火首帧（8ms）抬升 | 0.415° | **0.006°**（降到 1.4%） |
| 峰值角速度 | 52.4°/s，**在第 0 发** | **23.1°/s，在第 3.5 发** |
| 第 1 发累计 | 4.66°（远超靶半径） | 0.74°（仍在 0.9° 内，**首发照样命中**） |
| 总抬升 | 22° | 22°（不变） |

横向也加了淡入（`ramp = min(1, k/4)`）——直接从第 4 发开始摆会在那里出现斜率折角，同样是一下顿挫。

**22° 是个约数，不声称等于 AK 实测值。** 网上找不到可信的角度数值（资料全是定性描述），所以不按角度反推，改从玩家能验证的那一端定：普遍反映压一梭 AK 大概要下拉三四厘米，而典型灵敏度在 50 cm/360 上下 → 3.5/50×360 ≈ 25°。界面会把“本次要下拉几厘米”按你的设置算出来，对不上手感就改 `SPRAY_RISE` / `SPRAY_SWAY` 两个常量。

**弹道可视化**（测试者提的）：每一发在开枪那一刻记下准星指向的**世界方向**，画成弹孔并连成轨迹，打完一梭留在屏幕上到下一梭开始才清。首发标绿（游戏里首发是准的，可以当参照点）。压得住就聚成一团，压不住就拉成一条往上爬的线。

这顺带把一件容易被误读的事画明白了：**准星始终在屏幕正中、从不跟随后坐力**（相当于 `cl_crosshair_recoil 0`）。`drawCrosshair` 永远画在 `W/2, H/2`，任何时候都不加偏移；会动的是视角。

弹道必须**按阶段判断**才传给 `Scene.draw`：别的阶段 `trial.marks` 是 undefined，会退到残留的 `sprayMarks`，弹孔就渗到转身测试里去了。

**指标**：RMS 偏离（主）、纵向/横向分解、开枪瞬间命中率。纵向大 = 持续下拉没执行好；横向大 = 闭环修正跟不上。

**只在匹配度模式跑，不进 A/B 记分卡。** 记分卡是四维 + Holm 校正（当初把整体假阳性从 19.7% 压到 4.0% 靠的就是它），加第五维会让 Holm 更严、拖累现有四项的检出力——和波次指标是同一笔账。

**验证**（逐帧驱动真实代码，四种虚拟玩家）：

| 玩家 | RMS | 纵 | 横 | 命中 |
|---|---|---|---|---|
| 完美抵消 | 0.30° | 0.24° | 0.18° | 100% |
| 压过头 25% | 4.48° | 4.41° | 0.81° | 7% |
| 完全不动 | 18.2° | 17.9° | 3.16° | 3% |

（缓和曲线后的数。不压枪的人命中率从 0% 变成 3%，因为头几发在后坐力起来之前就打出去了 —— 这反而更像实机。区分度从 0.30° 到 18.2°，60 倍跨度，没丢。）

纵横分解确实能把“拉过头”归到纵向。开发中抓到过一个真 bug：靶初始偏移 1.2° 而靶半径只有 0.9°，完美抵消后厉害的人也永远命中 0%，命中率指标直接废掉。已改成 0.3°。

### 场景参照密度：“像准星在抖”的真正原因

测试者反馈压枪时“像是准星在抖”。准星实际上一直是固定的，问题出在**参照物**：

原来的线框房间网格间距 1.5、墙在 9 米外，准星周围那一格张角 **9.5°**。整梭子 climb 22° 也只跨两格，中心区域几乎没有细节在动。视觉系统找不到“世界在动”的证据，就把运动归因到唯一看得见的东西：准星。游戏里满屏贴图 + 枪模 + 枪口火光，从来不会有这个歧义。

（这大概率也是“不跟手”那条反馈的同一个根因——当时的判断就是“场景视觉参照太少”，同一个问题在两个阶段各冒了一次。）

**修法**：加 3580 个伪随机散点（固定种子，不构成额外变量）铺在四壁、地面、天花板。用散点而不是加密网格：等距细线会摩尔纹、会闪，散点像贴图颗粒，给的是纯粹的光流信号。

| | 修改前 | 修改后 |
|---|---|---|
| 准星 5° 内参照点 | **0**（网格间距 9.5°） | **22**，平均间距 1.89° |
| 每帧绘制耗时 | — | 0.15 ms（对应上限 ~6900 fps） |

三千多个点逐个 `fillRect` 比投影本身还贵，按大小分三档、每档攒成一条 path 填一次 —— 三次 fill 代替三千次。

同时加了**枪口火光**（视口底部偏右，每发一闪）。没有枪模的情况下，它是把“画面在动”归因到“枪在响”的最便宜线索 —— 少了它，同样的位移只会被读成屏幕不稳。

**后坐力强度改成界面可调**（高级设置，默认仍为 22°）。**没有同时调低默认值是故意的**：要是视觉和量级一起改，就分不出到底是哪一个解决了问题。先修参照，再请使用者重新判断；若仍偏大，用滑杆对着游戏调到一致，把那个数报回来再改默认值。

### 压枪试打场

后坐力强度是个“调完不打一遍就不知道什么样”的参数，而压枪排在第 7 个阶段 —— 想感受一下就得先跑完热身、甩枪、跟枪、微控、修正、波次六个阶段。**这个循环成本高到没人会去调，滑杆等于白给。**

高级设置里加了「试打（不计分）」按钮，直接进压枪：

- **不计分**：`record()` 在 `Test.lab` 下直接 return，数据不进 `results`，只留最后一梭给 HUD。
- **不推进阶段**：`phaseDone()` 在 lab 下恒返回 false，想打多少梭打多少梭。
- **场内直接调强度**：滚轮或 ↑↓，下一梭就是新手感。这是关键 ——
  要是还得退出去改再进来，循环成本只降了一半。指针锁定状态下这两种输入都收得到。
- **HUD 实时显示**：当前强度、按当前设置手要下拉多少 cm、上一梭的 RMS 和命中率。
- **Esc 是退出不是暂停**：`pointerlockchange` 在 lab 下走 `exitSprayLab()`，
  回设置页并滚到那个滑杆旁边，强度保留。

同类问题的一般形式：**凡是“看不到效果就不知道该调多少”的参数，都得配一个零成本的预览通道。**
准星那边本来就有实时预览画布，所以从来没人抱怨过。

### 新手模式的候选值：无畏契约会偏快

握法区间是生理量，**两个游戏共用同一组数**，没有按游戏调。但职业选手分布不同：

- CS：30–45 cm/360
- 无畏契约：eDPI 常见 200–400 → **33–65 cm/360**，整体慢得多

所以在无畏契约里自动候选值会偏快。这是已知的、**故意没有硬调**的（硬调就是拟合）。觉得不合适用「自定义候选值」直接填；扫描最终按实测表现选，起点偏一点不会让结论错，只是候选可能没覆盖到最优区间。

---

## 区间而不是点值

能驾驭的灵敏度本来就是一段区间，不是一个点。但**“区间”有两种，含义完全不同，混起来就是骗人**：

| | 它说的是 | 多测几次会不会变窄 | 描述的是 |
|---|---|---|---|
| **建议区间** | 最合适的值大概落在哪儿 | 会 | 测量精度 |
| **可驾驭区间** | 比这个跨度更小的差异你分辨不出来 | **不会** | **你本人** |

- 建议区间 = 把偏差的 95% 置信区间传播过去（`sens/(1+bias±ci95)`）。
- 可驾驭区间 = 主测阶段逐次落点反推出的节奏分布的**分位数**（p10/p25/p50/p75/p90）。
  用分位数而不是 ±k×sd：不必假设正态，也免得去挑一个 k（那就是拟合）。

结果页把两者**分开展示并标注区别**，判定标题从“微调到 0.55”改成“微调到 0.54–0.56”。新增「你能驾驭的区间」卡片，用一条分布条画出 80%/50% 区间、中位数和当前设置的位置。

**口径上的诚实声明**：那段散布里同时含手的抖动和停枪判定的测量噪声，两者拆不开，所以它是**外沿**而不是保证。要真正确认某个区间内表现无差别，得用 A/B 或新手扫描去实测。

另外两个模式本来就给区间：新手扫描的 `plateau`（与最优候选无显著差异的那一段）是**真正实测出来的**可驾驭区间；A/B 在四维均无显著差异时，现在会直接说“A–B 这一整段你都能驾驭”。

---

## 断点续测

暂停后可以退出，进度存在 `sensTest.session`，下次打开设置页顶部出现「上次没测完」卡，接着测。

**三个必须守住的点：**

1. **配置整套存、整套还原。** 中途改了 DPI 或灵敏度再续测，前后两半就不是同一个实验了。
   开发时真撞上了这个 bug：顺序写反了，`setGame()` 内部第一件事是 `readCfg()`，
   会把界面上的现值读回 cfg，把存档里的**共用字段**（DPI、画面比例、分辨率）冲掉
   —— 实测存档 1600 DPI 被界面上的 400 覆盖。**必须先 `setGame` 再覆盖 cfg**。
2. **阶段签名校验。** 存档里记下阶段 id 串（如 `warmup|main|track|...`），
   恢复时重算一遍对不上就**拒绝续测**。与其悄悄把两份不同结构的数据拼起来，不如让人重测。
3. **中断本身会影响数据。** 这个测试依赖适应动力学，隔一天再续，手的状态已经变了。
   每次续测记下间隔时长（`R.resumes`），结果页如实标出来；超过 30 分钟升级为警告色。
   不替用户下判断，只把间隔摆出来。

**存档体积**：遥测数组（帧间隔、输入滞留、事件间隔）满载能到八万个数，只保留最后 3000 个
—— 中位数不受影响，但续测后的性能统计以后半段为主，结果页会说明。实测存档 **23.8 KB**。

开新测试时如果存档还在，会先 confirm 一句，不默默盖掉。

---

## 配色

整套界面只吃 CSS 变量，换游戏时给 `<html>` 打上 `data-game` 属性整体换掉：

- **CS2**：深蓝底 `#0b1520` + 经典黄 `#f5b300`，顶部蓝色辉光
- **无畏契约**：近黑底 `#0d1117` + 品牌红 `#ff4655`，正文用它那个偏暖的米白 `#ece8e1`

`--ok / --warn / --bad` 是语义状态色，**两套主题共用、不跟着换**——无畏契约的强调色本身就是红的，若连 `--bad` 也跟着变，报错提示就分不出来了。

---

## 尚未定论的三个问题

这三项都是“两种做法都说得通、缺真人数据以致无法定论”。工具里的处理方式是两边都算、把判定交给数据，而不是替用户拍板。

### 1. 波次归一化：每度耗时 vs 吞吐量

取决于测试者的运动时间与距离是**线性**还是 **Fitts（对数）**关系：

- 线性 → 每度耗时正确（合成验证：两种极端打靶顺序都得 12.0 ms/°）
- Fitts → 吞吐量正确（合成验证：8.24 vs 8.47 bit/s）

工具已同时输出两者，并用逐波的 `(pathDeg, idSum, clearMs)` 比较拟合残差，结果页标注**「本次 XX 拟合更好」**。

### 2. "不跟手"是不是真延迟

测试人员反馈有延迟感，但从他们数据反推帧率是 **234 fps**，不是掉帧。

已加遥测，结果页显示：**渲染帧率 / 帧间隔 P95 / 掉帧% / 输入滞留（中位数 · P95）**。

- 输入滞留 3–5ms → 不是技术延迟，可能是场景视觉参照太少（线框网格），加密网格或加地面纹理即可
- 输入滞留 20ms+ 或帧间隔抖动大 → 真问题，值得动架构

### 3. 波次指标要不要进 A/B 记分卡

加了会让多重比较从 4 项变 5 项，Holm 校正更严，**拖累其余四项的检出力**。目前没有足够数据说明清屏时间在不同灵敏度之间真的有区分度，所以它暂时只单独输出、不进记分卡。

---

## 浏览器的硬限制

| | 状况 |
|---|---|
| 绕过系统指针加速 | ✅ `unadjustedMovement`，报告里 `lockMode: raw` |
| 高回报率不丢事件 | ✅ `getCoalescedEvents` |
| 帧率 | ✅ 实测 234 fps |
| **回报率 >1000Hz** | ❌ **时间戳分辨率 1ms，物理上测不了** |
| 标签页 OOM 崩溃 | ⚠️ JS 抓不到，只能抓自身异常 |

要突破回报率那道墙必须读原生 HID。**Tauri 套壳可解，现有 HTML/CSS/JS 一行不用改**，另加 Rust 侧通道即可。Electron/原生重写的收益不明确（见问题 2）。

---

## 已知仍不完美

- **微调次数偏低**：只统计"过冲后往回拉"，欠冲后继续同方向补进去不计。实测某轮命中率 93% 但首次停枪只有 23% 落在靶上，中间那 70% 的修正大半没被计到。
- **习惯灵敏度反推会向输入收缩**：同一只手（真实 43 cm/360）在 104/65/43/29/19 五种设置下测，报出 60/49/43/40/36。方向永远对，只还原真实差距的 62–82%。界面已标注为下限。

---

## 分发注意

- 必须 Chrome/Edge；Firefox/Safari 拿不到原始输入，数据作废
- 必须在**独立浏览器窗口**打开，预览面板/侧边栏会报 `WrongDocumentError`
- 首次打开有红框提醒改 DPI 和灵敏度（默认值是示例值）
- **先选游戏再填灵敏度**——两个游戏各存一份，切过去要重新确认
- 填「测试者标识」→ 会写进导出文件名，方便区分不同人
- 导出文件名带游戏前缀（`cs-sens-*` / `val-sens-*`），不会混
- 数据不上传，靠测试者点「保存成绩单」发回图片（需要原始数据时另有「原始数据」链接）
- 上次崩溃的堆栈会在下次打开时显示在设置页顶部，带「复制错误详情」按钮
- localStorage 键统一为 `sensTest.*`，和旧的单游戏版本不共享存档
